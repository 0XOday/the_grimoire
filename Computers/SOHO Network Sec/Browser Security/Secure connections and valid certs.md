 * TLS and digital certificates implement a secure connection
 * validates the the identity of the host running the site 
 * issued by a certificate authority 
 * CA root certs must be trusted implicitly 
 * 